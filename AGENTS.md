# AGENTS.md — video-toolkit contract reference (machine-first)

Format rules for this file: DOC-CONVENTIONS (bottom). Updating behavior anywhere in src/ obligates updating the matching table here in the same change — see EXTENSION RULES.

STATUS: milestones M1–M12 complete (M6 bridges, M7 audio-mix single-pass, M8 review-frames + transcribe concurrency, M9 VTT + ship gate, M10 follow-up closure, M11 precision transcription + transitions/overlay/loudness — whisper-cpp word timestamps, filler word precision, crossfade single-pass, transitions catalog, overlay-text, measure-loudness, MCP streamable-HTTP transport `mcp-serve`; M12 motion + format completeness — T17 crossfade kind allowlist = the FULL verified catalog (58 kinds, each parametrically render-verified; `custom` fenced), T19 `export-gif` terminal op (single-pass palette gif), T18 `zoom` motion op (Ken Burns zoom/pan on BOTH render paths, frame-exact duration invariance)) · M13 complete (M13 transport progress + overlay completion + throughput — T21 `image-overlay` op — ONE image burned over the composed output via an additional input on BOTH render paths + the gif path, single pass; T22 `render-batch` — bounded-parallel multi-plan renders via `mapBounded` with per-plan failure capture, jobs default = the FIRST plan's source benchmark renderConcurrency clamped 1..4 else 1; T20 SSE progress streaming — a `tools/call` carrying `_meta.progressToken` gets `200 text/event-stream` with throttled (≥ 250 ms AND ≥ 1.0 point) strictly-increasing `notifications/progress` (token echoed verbatim, `total: 100`) during `video_render`/`video_preview`, closed by the final response frame byte-identical to the plain path; no-token requests and stdio byte-identical; disconnect/DELETE never cancel renders) · M14 complete (progress completion + plan hygiene + hosted CI — T23 `video_render_batch` OVERALL progress over the same SSE sink; T24 `plan lint` seven-rule advisory engine + MCP `video_plan_lint` (tools/list 20); T25 hosted CI `.github/workflows/ci.yml` — push/PR to `master`: ubuntu-latest, node 20, apt ffmpeg, `npm ci` → typecheck → test, single job, no matrix/deploy/publish; fresh-checkout proof verifier-run (local clone: npm ci → tsc → test, 0 fail, engine/model-absent tests skip); pushing it to GitHub stays user/coordinator-directed (halt-list — the workflow first runs only after that push)) · M15 complete (progress polish + distributability + onboarding — T26 batch SSE `message` template `plan i/N (basename): P% — overall O%` over T23's aggregator (render/preview messages, CLI, no-token HTTP, stdio byte-identical); T27 npm packaging — `files` allowlist + `prepare` = build (bins ship 0755), `npm install -g github:Arrangedgodly/video-toolkit` documented, NO registry publish; T28 examples cookbook — `examples/` = 7 runnable plans + lavfi input recipes in `examples/README.md`, smoke `src/test/examples.test.ts`, ships in the tarball) · M16 complete (environment doctor — T29 `video doctor`: ONE deterministic machine-readable dependency diagnosis over the existing probe machinery — 12 checks in fixed order, pure broken/degraded/ok aggregation law, per-check impact + remediation; MCP `video_doctor`, tools/list 21) · M17 complete (stdio-transport progress — T30: a `tools/call` carrying `_meta.progressToken` over `video mcp`/`video-mcp` receives `notifications/progress` as stdout JSON LINES while the tool runs — the SAME shared sink/throttle as the SSE path (`progressToken` verbatim, `total:100`, strictly increasing, ≥ 250 ms AND ≥ 1.0 point), then the single final response line; no-token requests and every non-`tools/call` method stay byte-identical) · 477 tests (`npm test`, ~120 s; transcription tests skip without an engine — `say`/Handy/whisper-cli absent; the four overlay-text validate/render integration tests skip when the fixed font `/System/Library/Fonts/Helvetica.ttc` is absent — a macOS system path missing on linux CI; validate itself stays correct-by-error there (`OPERATION_INVALID` naming the font), and the pure builder/escaping units keep running everywhere) · binary `video` on PATH (`npm link` from a checkout, or `npm install -g github:Arrangedgodly/video-toolkit` — git installs run `prepare` = `npm run build`, compiling dist/ + chmod'ing the bins; the npm package is the `files` allowlist `dist`/`src`/`examples`/`AGENTS.md`/`README.md`/`LICENSE` — nothing repo-internal ships; `examples/` = the runnable cookbook, T28) · node ≥ 20, ffmpeg/ffprobe required.

## GLOSSARY (canonical terms — never synonymize)

- **plan** — versioned JSON declaring an edit; the only input to render.
- **observation** — timestamped JSON emitted by a worker; never rendered, never mutates source.
- **worker** — deterministic analysis producing an observation (detect-silence, detect-scenes, transcribe, detect-filler).
- **bridge** — deterministic observation→plan expansion (`plan --cuts-from`).
- **timeline op** — `trim`/`cut`: absolute source-time ranges; order-independent; trims union, cuts subtract.
- **transform op** — `speed`/`resize`/`volume`/`normalize-audio`/`audio-mix`/`overlay-text`/`image-overlay`/`zoom`: global, ≤ 1 each per plan.
- **transition op** — `crossfade`: TIMELINE-affecting global op (≤ 1 per plan) that joins the compiled keep-segments with fade transitions instead of hard cuts; shortens the output by (N−1)·duration; never combined with `audio-mix`.
- **export op** — `export-gif`: TERMINAL op (last position only, ≤ 1 per plan; the only order constraint in an otherwise order-independent plan) that changes the output FORMAT, not the content — first of an export-op class; never a `TransformOpType` member.
- **engine** — swappable transcription backend behind one interface (`src/analysis/transcribe/engines.ts`).
- **preview** — cheap render to `<output>.preview.mp4` (640 w, ultrafast, CRF 30); under an `export-gif` op it is instead a real `<output>.preview.gif` (the op's own width/fps bound the cost); can never touch the final path.
- **source-id** — cache key = sha1(absPath|size|mtimeMs); changed file ⇒ new id, no invalidation logic.

## COMMANDS

Stdout = compact single-line JSON unless `--pretty`. Progress/debug/errors → stderr. Global flags: `--pretty --debug --no-cache`; `-h`/`--help` prints usage and exits 0 — as the first token or after any command. Timestamps: seconds (float), 3 decimals in emitted JSON.

| command | args | own flags | stdout contract | cache |
|---|---|---|---|---|
| `inspect` | `<input>` | `--json` (raw ffprobe) | `{file, duration, video?:{codec,width,height,fps,bitrate,pixelFormat}, audio?:{codec,sampleRate,channels,bitrate}}` | metadata.json |
| `plan` | `<input>` | `--cuts-from <silence.json\|filler.json>` · silence: `--min-duration <s=0.5> --pad <s=0.25>` · filler: `--filler-pad-before <s=0.10> --filler-pad-end <s=0.25>` · `--highlights-from <highlights.json> --count <n=5> --min-score <0.35> --pad <s=0.5>` (one bridge per invocation; two → `OPERATION_INVALID`) | whole-source EditPlan (scaffold; agent refines); `--cuts-from` shape-discriminates the report (Zod: silence first, then filler; neither → `OBSERVATION_INVALID`) and expands it into `cut` ops (silence: min-duration-gated, pad kept each side; filler: every instance, pads expand beyond the instance times — exact word anchors when the report's `params.precision` is `words`, linear estimates within segment granularity when `segments` (verify with `extract-frame --at` in that mode)); `--highlights-from` replaces the whole-source trim with top-N highlight `trim`s (score desc, ties → earlier start; overlaps union; no qualifying candidate → whole-source scaffold unchanged) | metadata.json |
| `plan lint` | `<plan>` — collision rule: the literal first positional `lint` followed by a second positional selects lint; a lone `lint` positional is still a source path | — (`--pretty` global) | `{suggestions:[{code, operation?, message, fix?}]}` — advisory lint over a VALID plan (the judgment layer between validate and preview; workers propose, agents decide: suggestions are NEVER auto-applied, `fix` is advisory text, lint NEVER mutates the plan and NEVER changes validate's verdict); exit 0 ALWAYS on a valid plan (even with suggestions); an INVALID plan is a validate concern — validate's own report `{valid:false, errors[], warnings[]…}` on stdout, exit 1, the EXISTING codes, none new. Pure rule engine over plan JSON + the probed duration (the validate metadata probe, no new probes): deterministic, byte-identical per plan; ordering by `operation` (1-based declared; compiled-segment suggestions without an anchor sort last) then code, stable. Seven rules: **REDUNDANT_TRIM** (trim fully contained in the union of EARLIER trims — declared order names the redundant one; containment follows the compiler's own TOUCH_EPSILON-merge + MIN_SEGMENT-drop semantics, so a flagged trim contributes no compiled keep time) · **REDUNDANT_CUT** (cut removing ZERO kept time, evaluated against the incrementally SUBTRACTED segments — only the second of two covering cuts is flagged; a cut outside the kept trims is the same rule's "never kept" variant) · **MERGEABLE_TRIMS** (two trims separated by a gap ≤ **0.01 s**, touching included — the compiler auto-merges < 0.001 s (TOUCH_EPSILON) silently; the rule names the pair across the whole band either way; the LATER declared op anchors the suggestion) · **NOOP_VOLUME** (`db: 0` or `factor: 1` — parses, does nothing) · **NOOP_CUT** (cut removing kept time > 0 but < **0.01 s** = MIN_SEGMENT, incl. start/end equal at 3 decimals — subtraction changes nothing observable) · **OVERLAPPING_CUTS** (two cuts intersecting with positive length — pairwise, the LATER one flagged; the subtraction math handles overlaps, this is hygiene) · **SUBSECOND_SEGMENT** (a compiled keep-segment < **0.5 s** — likely a bridge/editing mistake; compiled not declared, so no `operation` anchor and sorts last; bounds at 3 decimals). SUGGEST_PREVIEW is OMITTED by recorded decision — unknowable from plan JSON + duration. MCP `video_plan_lint` (tools/list 20), payload = CLI stdout shape | none lint-owned (shares validate's metadata probe) |
| `validate` | `<plan>` | — | `{valid, errors[], warnings[], timelineDuration?, crossfadeDuration?, expectedDuration?}` — `timelineDuration` is the PRE-fade compiled timeline; with a `crossfade` op the report also carries the fade and the adjusted expectation `timeline − (N−1)·fade` (pre-speed); exit 0/1 | metadata.json |
| `preview` | `<plan>` | `--force` | RenderResult (mode preview) | metadata.json |
| `render` | `<plan>` | `--force --encoder libx264\|h264_videotoolbox --mode final\|preview` | RenderResult `{output, mode, encoder, timelineSegments, timelineDuration, outputDuration, wallMs, command[]}` — with an `export-gif` op `encoder` reports `gif` (the `--encoder` flag is ignored: the gif muxer has no h264 path) and the output is `.gif` | metadata.json |
| `render-batch` | `<plans...>` (plan files, directories, or `*` globs; every positional is a plan arg) | `--jobs <n> --force` | `{results:[{plan, ok, output?, result?, error?}…], summary:{rendered, failed, failures:[{plan, code, message}], wallMs, jobs}}` — each plan renders via `renderPlan` under `mapBounded` (bounded parallelism; per-plan failure CAPTURED and REPORTED as `error` = the ToolError object, never thrown — the batch always runs to completion; `output` = shorthand for `result.output` on success; results index-aligned to the EXPANDED input order regardless of completion order); exit 0 iff all succeeded, 1 otherwise; expansion: a directory arg = the `*.json` FILES directly inside (non-recursive, sorted by name; a directory named `*.json` is not a plan), a literal `*` arg matches within its own directory (`*` = any non-separator run; no `?`/`[]`/`**` — the shell remains the expansion surface for anything richer; matches sorted; no match → that arg contributes nothing), a literal path that does not exist is carried through and reported as its per-plan error (same never-stop semantics), per-arg expansions sorted with arg order preserved; jobs default = the FIRST plan's source cached `video benchmark` renderConcurrency recommendation when an integer in 1..4, else 1 (T5's `concurrencyFromBenchmark` derivation mirrored; plans may have different sources — the default is first-plan-derived); explicit `--jobs` must be an integer ≥ 1 (NO upper cap — the operator's own trade-off) else `OPERATION_INVALID`; no plans matched → `OPERATION_INVALID`; `--force` passes through to EVERY render (each render's own overwrite guards stay intact — the batch never bypasses `OUTPUT_EXISTS`/source protection); parallel renders are independent processes writing distinct outputs; per-plan progress lines `[i/N] <file>` on stderr; MCP `video_render_batch` same payload (per-plan failures never `isError`); over HTTP with `_meta.progressToken` the batch streams OVERALL progress (`video_render`/`video_preview`'s per-render percent replaced by `(Σ per-plan fractions)/N × 100` — not-yet-started plans 0, each fraction = the plan's last engine percent/100, a finished plan (success OR captured failure) LOCKED at 1; `message` reads the FIXED template `plan i/N (basename): P% — overall O%` — i = the triggering plan's 1-based index in the EXPANDED order, basename = `path.basename(planPath)` (the CLI stderr precedent; duplicate basenames acceptable — full paths ride the CLI lines + `results[]`), P = the plan's OWN current percent as a `Math.round` integer (its fraction — locked at 1 → `100` on its completion event), O = the overall as a `Math.round` integer; T26 — pure formatting over the T23 aggregator, `timeSec` still forwarded, `video_render`/`video_preview` messages + the CLI + no-token HTTP + stdio byte-identical; last completion = exactly 100, no synthetic final event) | metadata.json (per plan, via renderPlan) |
| `diagnose` | — | — | `{os, cpu, memory, binaries, ffmpeg:{hwaccels, encoders:{h264,hevc,av1}}}` | none |
| `doctor` | — | — (`--pretty` global) | `{status:"ok"\|"degraded"\|"broken", checks:[{name, status:"ok"\|"missing"\|"degraded", detail, impact?, remediation?}], engines:{handy, whisper}, counts:{ok, degraded, missing}}` — ONE deterministic, machine-readable diagnosis of every environmental dependency, assembled from the EXISTING tested probe machinery (binaryPath/ffmpegVersion/the hasFilter cache/listEncoders, findHandy, whisper's exported model-resolution helpers — no ad-hoc re-probes). Checks (fixed order): `node` (vs the manifest's engines.node — CORE) · `ffmpeg` · `ffprobe` (presence + version — CORE) · `filter-subtitles` (libass → the `captions` op) · `filter-drawtext` (freetype → the `overlay-text` op) · `encoders` (software `libx264` + hardware h264 via listEncoders → `render --encoder`/`benchmark`; hardware absent with libx264 present = degraded, libx264 absent = missing) · `overlay-font` (OVERLAY_FONT_FILE stat) · `engine-handy` (findHandy) · `engine-whisper` (whisper-cli + DEFAULT-model resolution across BOTH models dirs, with the found-models listing per dir) · `say` (informational — darwin speech fixtures only) · `cache-cwd` · `cache-toolkit-root` (`.video-agent` writable — touch + remove a `.doctor-probe` file: the command's ONLY write). Aggregation law (pure `aggregateDoctorChecks`): broken = a CORE check not ok (ffmpeg/ffprobe absent, node below engines — reported as check status `missing`, the required binary/version is missing); degraded = any optional check not ok; ok otherwise; counts = exact tallies. `engines.{handy,whisper}` = availability booleans (whisper = binary AND default model resolvable — the engine's own `detect()` law). Exit 0 unless broken (exit 1). MCP `video_doctor` (tools/list 21; the report is ALWAYS the payload — a broken environment is a diagnosis, never `isError`) | none (read-only probes; the cache probe file is removed) |
| `benchmark` | `<input>` | `--seconds <n=6>` | `{segmentSeconds, recommended:{encoder, renderConcurrency}, results[]}` (measured, not inferred) | benchmark.json |
| `transitions` | — | — | `{transitions:[{kind}], count, ffmpeg, note?}` — `note` ONLY on upgrade drift (this build lists kinds beyond the schema allowlist; extras named, unaccepted for `crossfade.kind` until re-verified). Catalog parsed live from this build's `ffmpeg -h filter=xfade` (ground truth, never a hand-maintained list; order = the help listing; the `custom` enum sentinel is excluded). Feeds `crossfade.kind`; T17's allowlist EQUALS this catalog on the pinned build (every kind parametrically verified: exit 0, duration law frame-exact, decodable output — a live catalog or cache hit MISSING an allowlisted kind → `FILTER_HELP_UNPARSEABLE`, offender named; an upgraded build listing MORE kinds stays a working catalog + the note). Duration guidance: 0.2–1.0 s typical; `fade` is the cheapest and the empirically validated kind; all kinds share duration/offset semantics | transitions-<ffmpeg-version>.json (cache id `env` — environment-keyed, no source fingerprint; an ffmpeg upgrade re-parses) |
| `detect-silence` | `<input>` | `--threshold <dB=35> --min-duration <s=0.5>` | SilenceReport | silence-t<thr>-d<min>.json |
| `detect-scenes` | `<input>` | `--threshold <0..1=0.4>` | SceneReport | scenes-t<thr>.json |
| `transcribe` | `<input>` | `--engine handy\|whisper-cpp --model <id\|path/name> --chunk <s=25> --no-snap --concurrency <n> --word-timestamps` | TranscriptReport — handy: windows run as bounded parallel jobs (n at a time; default = cached `video benchmark` renderConcurrency recommendation for this source+ffmpeg build when it is an integer in 1..4, else 1); report byte-identical to sequential (segments re-sorted by window start; concurrency NOT in the cache key — warm cache hits at any n); non-integer/<1 n → `OPERATION_INVALID`; any window failure fails the whole task (`TRANSCRIPTION_ENGINE_FAILED`). whisper-cpp: NATIVE segments from ONE whole-file invocation (no windowing — `--chunk`/`--no-snap`/`--concurrency` are no-ops); `--word-timestamps` requests per-word `segments[].words` and selects whisper-cpp when no `--engine` is given; `--word-timestamps` + a word-incapable engine (handy) → `OPERATION_INVALID`; whisper model resolution (ordered): explicit resolvable `--model` path > `.video-agent/models/<name>` in the cwd > the same name under the toolkit root's `.video-agent/models/` > default `ggml-base.en.bin` at the same two locations > `TRANSCRIPTION_ENGINE_UNAVAILABLE` with `details.known` listing BOTH models dirs | handy: transcript-<engine>-<model>-c<chunk>.json · whisper-cpp: transcript-<engine>-<model>-c0-w<0\|1>.json (c0 = unwindowed; w = the words flag — toggling it misses the warm cache; never collides with windowed keys) |
| `detect-filler` | `<transcript.json>` | `--words "um,uh,…"` | FillerReport — `params.precision`: `words` (segment carries `words`: phrases match over CONSECUTIVE words — case/punctuation-normalized, longest first, never across segment boundaries — anchored first-word start → last-word end, exact times) or `segments` (linear interpolation within each segment — the estimate); `words` only when EVERY segment carries word timings (one wordless segment drops the claim; word-bearing segments still use their exact times) | none (pure transform) |
| `find-highlights` | `<input>` | `--transcript <t.json> --keywords "a,b" --min-score <0.35> --count <5>` | HighlightReport | silence cache reused |
| `measure-loudness` | `<input>` | — | LoudnessReport — loudnorm FIRST-PASS measurement of the source audio (`inputI` LUFS integrated, `inputTP` dBTP true peak, `inputLRA` LU range, `inputThresh` LUFS threshold; values exactly as loudnorm emits them, NOT rounded) from ONE analysis invocation to the null muxer (`-vn`, `-f null -`) — an observation probe like benchmark's measurements, NOT a render: writes no media file (INVARIANT 3). Fixed invocation `loudnorm=I=-16:TP=-1.5:LRA=11:print_format=json` (= the `normalize-audio` render defaults; recorded as `params.targetI`). Agent use: gain to reach a target = target − `inputI` dB → feed `normalize-audio.target` (or a `volume` op) with the measurement as evidence. Source without audio → empty report `{duration, note:"no audio stream"}` (not an error); audio loudnorm measures as `-inf` (digital silence) → same empty shape with its own note; measurement JSON absent/garbled on stderr → `FFMPEG_FAILED` | loudness-i-16.json |
| `captions` | `<transcript.json>` | `--plan <p.json> -o <out.srt\|out.vtt> --format srt\|vtt` | `{output, cues, dropped, remapped}` — format from the `-o` extension (`.srt`\|`.vtt`, case-insensitive), `--format` overrides and rescues any extension; `.vtt` = WebVTT (`WEBVTT` header, `HH:MM:SS.mmm` `.`-separated ms, `&`/`<`/`>` escaped in cue text, cue numbers kept as ids); cue split/drop/remap math identical to SRT (byte-identical `.srt` behavior); with `--plan`, a `crossfade` op in the plan shifts each segment-j fragment by its cumulative join shrinkage (fragment in segment j shifts −(j−1)·fade — plain cues would drift late by exactly that); `-o` with another extension and no `--format` → `OUTPUT_PATH_INVALID`; no `-o` → `<base>[.edited].<fmt>` | none |
| `extract-frame` | `<input>` | `--at t1,t2 \| --count n=6 --size w --dir d` | `{dir, frames[]}` (jpg paths) | metadata.json |
| `review-frames` | `<input>` | `--scenes <scenes.json> --per-boundary <n=4> --window <s=1.5> --size <w=480> --dir d` | `{dir, groups:[{boundary, frames[]}]}` (jpg paths) — per-boundary stills evenly spaced (interval midpoints) in `[boundary − window/2, boundary + window/2]` clamped to `[0, duration]`, downscaled to `--size` width; boundaries fully outside the source drop; empty report → `groups:[]` + `note` (not an error); non-integer/`<1` `--per-boundary` or `≤0` `--window` → `OPERATION_INVALID`; unparsable scenes file → `OBSERVATION_INVALID` | none (fresh stills like generate-proxy; scenes caching stays in detect-scenes) |
| `generate-proxy` | `<input>` | `--width 480 --output f` | `{proxy, width, duration, wallMs, command[]}` | none |
| `mcp` | — | — | MCP stdio server (see W5): JSON-RPC, newline-delimited; a `tools/call` carrying `_meta.progressToken` (string or integer; non-conforming ⇒ treated as absent) receives `notifications/progress` LINES while the tool runs — one JSON object per `\n`, same shared sink/throttle/params as `mcp-serve`'s SSE frames (T30) — followed by the single final response line; every other request shape (incl. all no-token requests and non-`tools/call` methods) stays byte-identical: one response line, zero notifications | — |
| `mcp-serve` | — | `--port <n=8765> --host <h=127.0.0.1> --token <bearer>` | MCP streamable-HTTP server (W5; R4's committed minimal contract docs/ultron/research/r4-mcp-streamable-http.md + R6's progress extension docs/ultron/research/r6-mcp-progress-sse.md): ONE `/mcp` endpoint, plain-JSON replies (the server never pushes unsolicited — no GET stream), same 21 tools as stdio via the SHARED dispatcher; `POST` request → `200 application/json` (one JSON-RPC object; JSON-RPC-level errors — unknown method `-32601`, tool failures as `isError` results — ride in the 200 body) — EXCEPT a `tools/call` whose `params._meta.progressToken` is a string or integer (float/object/null/absent ⇒ treated as absent ⇒ plain path), which replies `200 text/event-stream` (`Cache-Control: no-cache`, `Connection: keep-alive`, `X-Accel-Buffering: no`, no Content-Length): every frame is `event: message` + `data: <one JSON-RPC object>`; `video_render`/`video_preview` emit `notifications/progress` (engine percent 0–100) and `video_render_batch` emits OVERALL progress ((Σ per-plan fractions)/N × 100 — not-yet-started 0, finished plans LOCKED at 1 success-or-failure, `message` reads `plan i/N (basename): P% — overall O%` naming the in-flight plan — T26; the aggregate + the message are computed in `src/render/batch.ts`, the transport gates unchanged, `video_render`/`video_preview` keep `rendering <t>s` via the sink's `p.message ??` fallback) — `{progressToken (echoed verbatim), progress (strictly increasing), total: 100, message}` throttled to ≥ 250 ms AND ≥ 1.0 progress-point apart; every other tool streams a response-only stream; the stream closes with exactly ONE final response frame byte-identical to the plain path's body (incl. every error path), then `end()`; a client disconnect mid-render stops the frames while the render FINISHES (disconnect is NOT cancellation) — progress is opt-in, no-token clients see zero change; notification/response → `202` empty (`notifications/cancelled` lands here, accepted and ignored — abort wiring is future work), `GET`/`HEAD`/other methods → `405` + `Allow: POST`, `DELETE` → `204` (drops the presented session; never aborts in-flight renders), other path → `404`; sessions OPTIONAL — `Mcp-Session-Id` (crypto-random UUID) issued on the `initialize` response, never required, dead id → `404` (the resync signal: re-initialize without it); transport failures: body not JSON → `400` `-32700`, unknown `MCP-Protocol-Version` header → `400` `-32600` (also the 2026-era clients' documented fallback-to-initialize trigger), non-local `Origin` → `403`, missing/wrong bearer → `401` + `WWW-Authenticate: Bearer`, body > 10 MB → `413` (all gates run BEFORE any stream opens); `initialize` echoes a known client `protocolVersion` else answers `2025-06-18` (stdio keeps its historical echo/`2024-11-05` default); explicit `Content-Length` on every plain reply (keep-alive framing; the SSE stream alone is chunked); listening URL printed to STDERR only (stdout stays clean; `--port 0` = OS-assigned, chosen port in that URL); non-loopback `--host` (`localhost`, `127.x.x.x`, `::1` are loopback) without `--token` → exit 1 `OPERATION_INVALID`, refuses to listen | none |
| `help` | — | — | usage text on stdout, exit 0 — also reached by `-h`/`--help` (first token, or after any command: global usage, exit 0); bare `video` (no command) prints the same usage on stdout and exits 2, nothing on stderr | — |
| `version` | — | — | `{toolkit:"0.1.0", ffmpeg:"<probed>"}` single-line JSON on stdout, exit 0; ffmpeg version probed live at call time (uncached) | none |

## WORKFLOWS (proven sequences; steps are the contract)

- **W1 basic edit**: `inspect` → `plan > plan.json` → edit `operations` → `validate` until valid → `preview` → iterate → `render` once.
- **W2 tighten silence**: `detect-silence > silence.json` → `plan --cuts-from silence.json > plan.json` (tune `--min-duration`/`--pad` = the editorial decisions) → prune cuts by judgment → W1 tail.
- **W3 speech review**: `transcribe > transcript.json` → `detect-filler transcript.json > filler.json` → `plan <input> --cuts-from filler.json --filler-pad-before 0.10 --filler-pad-end 0.25 > plan.json` (tune the pads = the editorial decisions) → prune cuts by judgment → W1 tail. Filler times are exact word anchors when the transcript carries word timings (`params.precision: "words"` — whisper-cpp `--word-timestamps`); linear estimates within segment granularity otherwise (`"segments"` — verify with `extract-frame --at` before rendering).
- **W4 strategy**: `diagnose` → `benchmark` → pass `--encoder` to render accordingly. For audio loudness: `measure-loudness` → pick `normalize-audio.target` (or compute the `volume` gain = target − `inputI` dB) with the measurement as evidence → W1 tail.
- **W5 MCP**: `video mcp` (stdio; or bin `video-mcp`) or `video mcp-serve` (streamable HTTP — `http://127.0.0.1:8765/mcp`, flags `--port/--host/--token`; token REQUIRED for a non-loopback `--host`); tools = `video_<command_snake_case>`; results identical to CLI stdout; failures = `isError:true` + `{error:{code,message,details}}`. HTTP client config (ZCode-style): `{"type":"http","url":"http://127.0.0.1:8765/mcp","headers":{"Authorization":"Bearer …"},"timeoutMs":…}`; stdio client config: `{"type":"stdio","command":"video","args":["mcp"]}`. Progress is OPT-IN on BOTH transports (T30): send `_meta.progressToken` (string or integer; float/object/null/absent ⇒ treated as absent) on a `tools/call` and you receive throttled (≥ 250 ms AND ≥ 1.0 point — the ONE shared sink in `src/agent/mcp-http.ts`) strictly-increasing `notifications/progress` (`progressToken` echoed verbatim, `total: 100`) during `video_render`/`video_preview` (engine percent) and `video_render_batch` (OVERALL = (completed + Σ in-flight fractions)/N × 100 — a finished plan counts 1 whether it succeeded or was captured as a failure; the last completion reaches exactly 100; each event's `message` names the in-flight plan — `plan i/N (<basename>): P% — overall O%` with i the 1-based expanded index, both percents `Math.round` integers — while `video_render`/`video_preview` events keep `rendering <t>s`). Framing differs by transport only: HTTP answers that call with an SSE stream (`text/event-stream`, `event: message` frames) closing with the final response frame byte-identical to the plain path; stdio emits the same objects as stdout JSON LINES (one per `\n`) followed by the single final response line — notifications always precede their response. Non-progress tools and non-`tools/call` methods (e.g. `tools/list` with a token) stay response-only on both. SDK clients should set `resetTimeoutOnProgress: true` so each event refreshes the request timeout (the concrete fix for minute-long renders AND long batches, on either transport); no-token requests stay byte-identical (single response line/JSON body, zero notifications); a mid-render HTTP disconnect or `DELETE` never cancels a render (`notifications/cancelled` is accepted-and-ignored; abort wiring is future work).
- **W6 highlight compilation**: `transcribe > transcript.json` → `find-highlights <input> --transcript transcript.json > highlights.json` → `plan <input> --highlights-from highlights.json > plan.json` (tune `--count`/`--min-score`/`--pad` = the editorial decisions) → prune trims by judgment → W1 tail. Deterministic bridge per GLOSSARY; the agent decides.
- **W7 scene review**: `detect-scenes > scenes.json` → `review-frames <input> --scenes scenes.json > review.json` (tune `--per-boundary`/`--window` = the editorial decisions) → inspect the grouped jpgs → express judgments as `trim`/`cut` ops → W1 tail. Still groups, not a proxy, are the review unit.
- **Examples cookbook**: runnable plans for the flagship workflows live in `examples/` (7 plans: silence-tighten, filler-cut, highlights, watermark-captions, montage-crossfade-zoom, gif-preview, audio-mix) with one-line index + input recipes in `examples/README.md` — inputs regenerate from ffmpeg lavfi one-liners (speech via `say` on darwin, lavfi fallback everywhere); stable relative input names so plans validate from any directory holding the generated inputs; v1 avoids `overlay-text` (the macOS-fixed font) so every example runs on linux CI; smoke-validated by `src/test/examples.test.ts` (validate + `plan lint` + one preview per example).

## PLAN SCHEMA v1

`{version:1, source, operations[], output:{path, mode:"final"|"preview"}}` — Zod source of truth: `src/core/schemas.ts` (do not restate here). Timeline math: `src/core/timeline.ts` (pure, unit-tested).

| op | keys | semantics |
|---|---|---|
| `trim` | `start,end` (s) | keep range; multiple trims union |
| `cut` | `start,end` (s) | remove range from keeps |
| `speed` | `factor` (0<f≤10) | global; audio follows (atempo-chained) |
| `resize` | `width[, height]` (px) | global; width-only keeps aspect |
| `volume` | `db` XOR `factor` | global; factor→dB = 20·log₁₀(f) |
| `normalize-audio` | `target?` (LUFS, default −16) | single-pass loudnorm |
| `captions` | `file`, `style?` (ASS force_style) | burns .srt in the same pass. **CUE TIMES ARE OUTPUT-TIMELINE**: source-timed srt must be remapped first via `video captions --plan` (cues spanning cuts split; fragments <0.3 s drop) |
| `overlay-text` | `text` (literal, ≤1024 chars) · `from?`/`to?` (s, OUTPUT-timeline visibility window; both absent = always visible) · `position? top\|center\|bottom` (default `bottom`) · `fontsize?` (px, int 1–512, default 48) · `color?` (name or `0xRRGGBB[AA]`, default `white`) · `box?` (default `true` → `box=1:boxcolor=black@0.5:boxborderw=12`) | ONE drawtext burned in the SAME pass, after scale+subtitles (fontsize in output px; x centered `(w-text_w)/2`; y top `h*0.1` / center `(h-text_h)/2` / bottom `h-text_h-h*0.1`). Text is LITERAL: drawtext crosses TWO unescaping stages (filtergraph + option-value tokenizer) → own escaping (`\`×4, `'`×3, `:`×2, `,;[]`×1; `%` raw under `expansion=none`), unit-proved pixel-identical vs `textfile=` ground truth. Font FIXED `/System/Library/Fonts/Helvetica.ttc` (font or `drawtext` filter missing → `OPERATION_INVALID`); `from ≥ to` → `RANGE_NEGATIVE`; `to` > expected output duration (timeline/speed) → `OPERATION_INVALID` |
| `image-overlay` | `file` (image path, required; png-with-alpha is the documented contract) · `position?` `top-left`\|`top-right`\|`bottom-left`\|`bottom-right`\|`center` (default `bottom-right`) · `width?` (px, int 1–16384; scales the overlay KEEPING aspect; absent = native size) · `opacity?` (0 < o ≤ 1, default 1) · `from?`/`to?` (s, OUTPUT-timeline enable window; both absent = always visible — overlay-text semantics) | ONE image (watermark/logo bug beside `overlay-text`) burned over the composed output in the SAME pass: the image rides as an ADDITIONAL `-i` input (the audio-mix second-input precedent; a still frame overlays with the filter's default repeat-at-eof semantics) feeding ONE `overlay` filter appended at the drawtext composition point — after zoom/scale/subtitles/drawtext, before `format=yuv420p`/the gif palette graph — on the select path, the xfade chain (last link parks at `[vc]`), AND the gif path (overlay BEFORE the palette suffix, so the enable window addresses the FULL output timeline). Anchors on the OUTPUT frame: the watermark does NOT zoom with the content. Overlay input prep (uniform, one shape): `[scale=W:-1,]format=rgba,colorchannelmixer=aa=<o>` — recorded decisions: scale height `-1` (exact aspect; even-height `-2` is an encoder constraint on the MAIN stream only), `format=rgba` on the overlay input ONLY (RGBA over yuv main — measured), `aa=` always emitted (aa=1 is a no-op). Positions use the filter's own W/H (main) w/h (overlay) constants with a FIXED 16 px margin (recorded: constant inset, not min(W,H)·0.05): top-left `16:16` · top-right `W-w-16:16` · bottom-left `16:H-h-16` · bottom-right `W-w-16:H-h-16` · center `(W-w)/2:(H-h)/2`. The plain select path flips into `-filter_complex` for the op (a `-vf` chain cannot reference a second input; audio keeps its `-af` chain via `-map 0:a`, measured accepted); with `audio-mix` the image is the LAST input (after the bed) inside ONE shared `-filter_complex`; on the chain path the image is input n. Bounds: file missing → `OPERATION_INVALID` (details: operation, path); opacity ≤ 0 or > 1 → `OPERATION_INVALID`; `from ≥ to` → `RANGE_NEGATIVE`; `to` > expected output duration ((timeline − crossfade shrinkage)/speed — the overlay-text `to` bound) → `OPERATION_INVALID`; ≤ 1 per plan. Placement pixel-verified at all five positions |
| `audio-mix` | `file` (music bed, required) · `level?` (dB, −60…0, default −18) · `duck?{threshold?, ratio?, attack?, release?, makeup?}` — `threshold` is LINEAR amplitude 0.000976563–1 (default 0.02 ≈ −34 dB; NOT dB), `ratio` 1–20 (default 8, ≈18 dB measured depth), `attack`/`release` ms (defaults 20/400), `makeup` 1–64 | music bed mixed under the plan audio with speech-keyed sidechain ducking, in the SAME single pass (bed is a `-stream_loop` 2nd input, loops and trims to the timeline, conforms to the speech's own rate/layout; `amix duration=first` natural end — no `-shortest`); bed missing → `MIX_INPUT_NOT_FOUND`; source without audio → `NO_AUDIO_STREAM` warning and the op is a no-op |
| `crossfade` | `duration` (s, required, > 0) · `kind?` (default `fade`; allowlist = the FULL verified catalog — 58 kinds on this build, EVERY one parametrically verified to render: exit 0, duration law frame-exact, decodable output; compile-time `as const` Zod enum (never read from the live catalog at parse time); allowlist == catalog enforced by tests in BOTH directions; `video transitions` emits the catalog. FENCE: `custom` parses but is ALWAYS rejected `OPERATION_INVALID` naming `expr=` — the xfade sentinel is not a standalone transition) | joins the compiled keep-segments with crossfade transitions (video `xfade` + audio `acrossfade`) in the SAME single pass — N `-ss/-t -i <source>` inputs, chained fades at offsets O_k = Σ L_i ≤ k − k·duration (graph validated frame-exact in docs/ultron/research/r3-xfade-single-pass.md). **Output duration = timeline − (N−1)·duration** (the one canonical law: `validate` reports it as `expectedDuration`, render progress/verify consume it, overlay-text `to` is bound by it). Speed/scale/subtitles/overlay-text/loudnorm/volume compose AFTER the chain (speed = `setpts=PTS/f` + `atempo`; NO speed-aware offsets). Bounds (all `OPERATION_INVALID`, client-side because ffmpeg exits 0 with corrupted output when violated): `duration` ≥ max(0.05 s, 1 frame) — 2 frames is the perceptible minimum; `duration` < EVERY keep-segment length (shortest segment named in the error); ≥ 2 keep-segments (single-segment timeline → error, never a silent no-op); ≤ 1 per plan; crossfade + `audio-mix` in one plan → error (unvalidated composition). Source without audio → video-only chain + `-an` (op stays valid). Captions: ALLOWED — `video captions --plan` shifts each segment-j cue fragment by −(j−1)·duration (outcome (a), measured honest; plain cues drift late by exactly k·duration); fragments never span joins; adjacent-segment fragments may overlap ≤ duration across a fade window (both true on screen); same-text fragments merge across a join |
| `zoom` | `mode?` (`in`\|`out`\|`left`\|`right`\|`up`\|`down`, default `in`) · `factor?` (default `1.2`; STRICT `1.0 < f ≤ 2.0` — comfortable ≤ 1.3, 1.5 already soft on text, 2.0 halves detail) · `easing?` (`smooth`\|`linear`, default `smooth` = smoothstep 3p²−2p³, the classic Ken Burns ease) | Ken Burns camera motion in the SAME single pass (graph + expressions measured in docs/ultron/research/r5-zoom-motion.md). `mode` is CAMERA direction: pan `right` = the view moves right = the CONTENT drifts left (measured ~107 px traverse at 1.2 on 640 w). VISUAL-ONLY — zoom never enters a duration law (`timelineDuration`/`expectedDuration`/progress/verify/overlay bounds all unchanged). zoompan discipline (hardwired, never plan params): `d=1` + `fps=<probed source fps>` + `s=<source WxH>` — this build's defaults `d=90`/`fps=25`/`s=hd720` are silent footguns (×108 duration / re-time / resize); ABSOLUTE `on`-frame expressions with the `min(on/(N−1),1)` clamp — the classic incremental `zoom+step` recipe is a MEASURED silent no-op with `d=1` on this build and `pzoom+step` runs away; neither is ever emitted. Position: select path — ONE zoompan between `select` and the retime `setpts` (zoompan DISCARDS input PTS, so speed composes after), one continuous ramp N = max(2, round(timeline×fps)) across the whole program (motion glides through hard cuts); crossfade path — one zoompan per input BEFORE the xfade links (ALL inputs uniformly; mixed zoompan/plain fails loudly), each with its own N_k = max(2, round(L_k×fps)) — motion re-runs its full ramp per segment and RESETS at each join (the standard per-clip Ken Burns treatment; the fade itself carries the reset; for pans the reset is a spatial jump blended by the fade). Pans: constant zoom `factor`, full-range traverse `(1−1/f)·W`, with a ×2 prescale before zoompan (PAN modes only — kills the native-res stall/jump judder; zoom modes stay unprescaled, center-anchored ±1 px). zoompan crops `iw/f × ih/f` and upscales back, so the pipeline resolution is stable (captions/overlays anchor correctly). Compositions validated: speed, resize, captions, overlay-text, audio-mix (select path), crossfade chain, export-gif (zoom rides before the palette graph); crossfade + audio-mix stays FORBIDDEN (the T12 fence subsumes it). Fences (all `OPERATION_INVALID`): factor ≤ 1.0 (no-op) or > 2.0 (detail halved); a motion unit shorter than 2 source frames (timeline unit on select plans; EVERY segment unit on crossfade plans); > 1 zoom per plan |
| `export-gif` | `width?` (px, int 1–16384, default 480 — vedit's proven value) · `fps?` (> 0, ≤ 60, default 12) · `from?`/`to?` (s, OUTPUT-timeline sub-range; both absent = the whole output) | TERMINAL export op (first of the export-op class): renders the compiled OUTPUT timeline (post trim/cut, crossfade-adjusted, post-speed) to an animated GIF in the SAME single invocation — the palette is generated INSIDE the one `-filter_complex` via `split` (recipe proven in vedit's build_gif, vedit.py:363-375): the from/to window as an output-timeline `trim` BEFORE `fps`, then `fps=N,scale=W:-2:flags=lanczos,split[a][b];[a]palettegen=stats_mode=diff[p];[b][p]paletteuse=dither=bayer:bayer_scale=5` appended AFTER the composed video chain (select or xfade chain → zoom/scale/subtitles/overlay; `-2` height keeps aspect, even). bayer dither is ordered/deterministic. The classic two-pass palette workflow is NEVER used (would break INVARIANT 1). GIF carries no audio: audio is dropped by design — an audio-bearing source emits a `GIF_AUDIO_DROPPED` validate WARNING (not an error); `volume`/`normalize-audio` under export-gif are no-ops (audio plumbing never built); `export-gif` + `audio-mix` → `OPERATION_INVALID` (the bed would be a silently dropped no-op — the crossfade+audio-mix fence precedent). Bounds: LAST operation (`OPERATION_INVALID` otherwise — the plan's only order constraint); ≤ 1 per plan; `from < to` else `RANGE_NEGATIVE`; `from`/`to` ≤ expected output duration ((timeline − crossfade shrinkage)/speed — the same canonical expectation overlay-text consumes) else `OPERATION_INVALID`; `output.path` MUST end `.gif` (case-insensitive) else `OUTPUT_PATH_INVALID`, enforced in validate AND the builder. Preview renders a real `<output>.preview.gif` (INVARIANT 2 holds; the op's own width/fps bound the preview cost — the mp4 preview's 640w is not applied; a `resize` op still composes before the gif scale). `--encoder` ignored (gif muxer; `RenderResult.encoder` = `gif`). h264 flags/movflags omitted on the gif branch. Practical note: gif wall-time/size grow with window length — use `from`/`to` for loops |

Rules: ops order-independent (EXCEPTION: `export-gif` is terminal — last position only) · ≤1 of each transform, ≤1 `crossfade`, ≤1 export op · renders are ONE ffmpeg pass (select-based multi-range, or the crossfade transition chain when a `crossfade` op is present with ≥2 segments; an `export-gif` appends the palette graph inside the SAME pass) · preview path ≠ final path · `--force` required to overwrite explicit outputs · source can never be an output.

## ERROR CODES (branch on `code`, fix, retry)

`PLAN_INVALID_JSON` plan not JSON · `PLAN_SCHEMA_INVALID` Zod fail (details.issues[]) · `SOURCE_NOT_FOUND` · `TIMESTAMP_OUT_OF_RANGE` op beyond source duration · `RANGE_NEGATIVE` start ≥ end · `EMPTY_TIMELINE` ops remove everything · `OPERATION_INVALID` duplicate transform / volume param / crossfade bounds (sub-floor fade, fade ≥ a keep-segment, <2 segments, crossfade+audio-mix) / crossfade kind `custom` (the xfade `expr=` sentinel — not a standalone transition) / export-gif non-terminal position, window beyond expected output duration, or export-gif+audio-mix / image-overlay missing image file, opacity outside 0 < o ≤ 1, or window beyond expected output duration / zoom factor outside 1.0 < f ≤ 2.0 or a motion unit (timeline, or any segment on a crossfade plan) shorter than 2 source frames / unknown MCP tool / `mcp-serve` non-loopback `--host` without `--token` · `MIX_INPUT_NOT_FOUND` audio-mix bed file missing (details: operation, path) · `OUTPUT_PATH_INVALID` (bad parent dir; captions `-o` extension; export-gif with `output.path` not ending `.gif`) · `OUTPUT_WOULD_OVERWRITE_SOURCE` · `OUTPUT_EXISTS` (use --force) · `FFMPEG_FAILED` (details.command + stderrTail) · `FFMPEG_NOT_FOUND` · `FFPROBE_NOT_FOUND` · `UNSUPPORTED_MEDIA` · `OBSERVATION_INVALID` observation file unparsable · `TRANSCRIPTION_ENGINE_UNAVAILABLE` (details.probed[]) · `TRANSCRIPTION_ENGINE_FAILED` · `FILTER_HELP_UNPARSEABLE` (`transitions`: xfade help absent/garbled for this build, or the parsed/catalog-cached enum lacks an allowlisted crossfade kind — details.missing[]).

## OBSERVATION SCHEMAS

Zod source of truth: `src/core/schemas.ts`. Shapes: SilenceReport `{segments:[{start,end,duration}], duration?, note?, params?}` · SceneReport `{boundaries:[{timestamp,confidence}]…}` · TranscriptReport `{segments:[{start,end,text,words?:[{start,end,text}]}], engine?, model?, language?, params?}` (`words` present only from whisper-cpp `--word-timestamps` — absence means segment granularity only; `params.wordTimestamps` records the flag on the native path; windowed params carry `chunkSeconds`/`snapToSilence` instead) · FillerReport `{instances:[{start,end,phrase,context}], params?{phrases, precision}}` (`precision: "words"` = exact anchors from `segments[].words`, `"segments"` = linear interpolation; `"words"` only when every segment carries words; phrases never match across segments) · HighlightReport `{candidates:[{start,end,score,text,reasons[]}], params?}` (score = weighted speech-rate 0.45 / pause-before 0.2 / keyword 0.25 / length-band 0.1, renormalized when keywords unset; deterministic proposal — the agent decides) · LoudnessReport `{inputI?, inputTP?, inputLRA?, inputThresh?, duration?, note?, params?{targetI}}` (loudnorm first pass; values exactly as loudnorm emits them — LUFS/dBTP/LU, not rounded; all four measurement fields are ABSENT when there is nothing to measure: audio-less source or digital silence (loudnorm `-inf`) return `{duration, note}`).

## ENGINES (transcription)

- **handy** (implemented; priority 1 — the default): binary `/Applications/Handy.app/Contents/MacOS/handy` (or `~/Applications/…`). Headless: `-f <16kHz-mono.wav> --json [--model <id>]`. stdout = single JSON `{text, model, best_ms, audio_secs, rtf,…}`; logs → stderr; **exits in `-f` mode; `--list-models` mode never exits — always wrap with timeout**. Default model on this machine: `parakeet-tdt-0.6b-v3` (Q8_0, Metal; 5–21× realtime measured).
- **whisper-cpp** (implemented; priority 2): binary `whisper-cli` on PATH (whisper-cpp 1.9.2, ggml 0.22.0 BLAS + Metal, flash-attn on). Native segments: `whisper-cli -m <resolved-model> -f <16kHz-mono.wav> -ojf -of <tmp-prefix>` writes exactly ONE file `<tmp-prefix>.json`; stdout = human segment lines, stderr = ggml/Metal logs (both ignored — the JSON file is the only channel); NO `-l`/`-wt`/`-ml`/`-sow`/`-dtw`/`-nfa` flags (measured harmful or no-ops — R2 candidate table, docs/ultron/research/r2-whisper-word-timestamps.md). Parse contract: `transcription[].{offsets{from,to} MS, text, tokens[]}`; words = leading-space tokens (skip `[_*` specials; punctuation/BPE tails append and extend end; zero-width `end:=next.from` in-segment, else left zero — never invented); ms÷1000 at 3 decimals. Exit 0 ok; any non-zero → `TRANSCRIPTION_ENGINE_FAILED` with stderr tail (availability is probed before spawn). Timeout `60 s + 2×duration` (≥11× headroom over worst measured RTF 0.177); RTF 0.06–0.10 warm (10–16× realtime) for `ggml-base.en.bin`. Models (ordered; R2's table EXTENDED with a second location — showcase defect fix, cwd-only resolution failed from any non-repo cwd): explicit resolvable `--model` path > `.video-agent/models/<name>` in the cwd > the same name under the toolkit root's `.video-agent/models/` (root derived from the module's own location, not the cwd) > default `ggml-base.en.bin` at the same two locations > `TRANSCRIPTION_ENGINE_UNAVAILABLE` with `details.known` sorted `*.bin` listing of BOTH models dirs (paths prefixed by their dir; whisper-cli does NO name magic and NO download — the engine always passes an explicit `-m`); `detect()` = binary on PATH AND default model resolvable (either location), detail names both facts.
- **Windowing split** (the orchestrator honors the capability): engines WITHOUT `transcribeSegments` (handy) are windowed by the toolkit (chunk default 25 s, boundaries snapped to silence gaps ±chunk/3, exact times by construction; 1 h source ≈ minutes; cached per source+engine+model+chunk). Engines WITH `transcribeSegments` (whisper-cpp) run UNWINDOWED — ONE whole-file 16 kHz mono WAV invocation, native segments + optional words; `--chunk`/`--no-snap`/`--concurrency` are no-ops (R2 measured windowing as pure overhead: 15.7–16.2× realtime unwindowed on 55.75 s).
- **Parallel windows** (safe + deterministic): `--concurrency <n>` runs per-window WAV extraction + engine invocations as bounded parallel jobs — engine invocations are independent processes (each loads its own model; keep n conservative on 16 GB; tmp WAVs unique per window index). Results are reassembled in window order, so out-of-order completion cannot reorder segments; the lowest-index window failure fails the whole task; report byte-identical to sequential (measured 1.8× wall-time at n=2 vs 4.95 s sequential on a 26 s source, 2026-09-03). Default n = cached `video benchmark` renderConcurrency for the source when an integer in 1..4, else 1.
- Add an engine: implement `TranscriptionEngine` (`src/analysis/transcribe/engines.ts`), append to `ENGINES` (priority = agent-efficiency order), parser unit test, update this table.

## ENVIRONMENT FACTS (this machine; re-run `video diagnose` to confirm)

- ffmpeg 9.0.1 **ffmpeg-full** (libass + freetype present → burned captions + overlay-text work; `subtitles`/`drawtext` filters available) via `brew unlink ffmpeg && brew link --force ffmpeg-full` (ffmpeg-full is keg-only; reverse with unlink ffmpeg-full + link ffmpeg; scrcpy keeps working — full build is a functional superset). videotoolbox hwaccel + h264/hevc encoders present.
- `overlay-text` font is FIXED: `/System/Library/Fonts/Helvetica.ttc` — verified present and rendering with this build's freetype (the op's ONE deterministic font rule; missing → `OPERATION_INVALID`, so font-less machines (linux CI) are correct-by-error BY DESIGN — the integration tests skip there via the test-tree `overlayFontAvailable` guard, the engine never guesses a fallback font).
- Note: the full 9.0.1 bottle renders ~2–3× slower than the old minimal 8.1.2 did (verified in test timings); benchmark cache is keyed by ffmpeg version so `video benchmark` re-measures after upgrades.
- Apple M2, 8 cores, 16 GB. Re-run `video benchmark <input>` for current per-source recommendations.

## INVARIANTS (violating these is a bug)

1. One ffmpeg pass per render — no intermediate encodes.
2. Source is never written; explicit outputs need `--force`; preview never writes the final path.
3. Workers never render; render never reads observations (plans are the only bridge).
4. All timestamps: seconds, float, emitted at 3 decimals.
5. Child processes: argv arrays only; engines wrapped with timeout.

## EXTENSION RULES (self-enforcing — do the doc update in the same change)

new command → COMMANDS row + cli case + tests · new plan op → PLAN SCHEMA row + validate rules + builder + tests · new worker → COMMANDS row + schema + runner + tests · new error code → ERROR CODES · new engine → ENGINES · behavior change → the one table that states it.

## DOC-CONVENTIONS

Machine-first: stable ALL-CAPS section keys · tables with fixed columns · literal JSON · units explicit (s, dB, px, LUFS, ×) · one canonical term per concept (GLOSSARY) · facts owned by code (`--help`, Zod schemas) are pointed at, never restated · every line carries a fact; no prose padding · examples must be runnable verbatim.
